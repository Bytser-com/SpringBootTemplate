apiVersion: v1
kind: Service
metadata:
  name: example-api
spec:
  type: LoadBalancer
  selector:
    app: example-api
  loadBalancerIP: 192.168.10.50   # must be in MetalLB IP pool and unused
  ports:
    - name: http
      port: 8080
      targetPort: 8080
